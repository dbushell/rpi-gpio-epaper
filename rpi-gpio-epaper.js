const {EPD, logT, logE, log1, log2, log3, log4, sleep} = require('./lib/epd');

module.exports = {
  EPD: EPD,
  logT: logT,
  logE: logE,
  log1: log1,
  log2: log2,
  log3: log3,
  log4: log4,
  sleep: sleep
};
